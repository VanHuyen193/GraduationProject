\documentclass[12pt,a4paper]{article}



% 1. Thiết lập Font và Ngôn ngữ (Dùng với trình biên dịch XeLaTeX)
\usepackage{fontspec}
\usepackage{polyglossia}
\setmainlanguage{vietnamese}
\setmainfont{Times New Roman}
\setdefaultlanguage{vietnamese}

\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{calc,positioning,arrows.meta}
\usepackage{pgfplots}
\pgfplotsset{compat=1.17}
\usepgfplotslibrary{groupplots}
% BẮT BUỘC cho các dải ±1 độ lệch chuẩn ở Chương 4. Thiếu dòng này thì mọi lệnh
% \addplot ... fill between[...] bị bỏ qua trong IM LẶNG: tài liệu vẫn biên dịch
% thành công, không một cảnh báo nào, chỉ có điều toàn bộ dải mờ biến mất khỏi
% hình vẽ. Kiểm tra nhanh: trong main.log phải có dòng "fill between: activating
% layered graphics"; nếu đếm được 0 dòng như vậy thì các dải đã không được vẽ.
\usepgfplotslibrary{fillbetween}
% Chú giải đặt thành một hàng ngang phía trên khung vẽ, nằm ngoài vùng dữ liệu
% nên không bao giờ đè lên đường cong. Dùng chung cho mọi biểu đồ ở Chương 4.
\pgfplotsset{
    legendabove/.style={
        legend style={
            at={(0.5,1.02)}, anchor=south,
            legend columns=-1,
            font=\small,
            draw=gray!40,
            /tikz/every even column/.append style={column sep=10pt},
        },
    },
}
\usepackage{changepage}
\usepackage{longtable}
\usepackage{array}

% 2. Các gói lệnh cơ bản
\usepackage{caption}
\usepackage{multirow}
\usepackage{float}
\usepackage{anyfontsize}
\usepackage{setspace}
\usepackage{tocloft,calc}
\usepackage{booktabs}
% Căn lề theo quy chuẩn ĐAKLTN: trên 2,5cm - dưới 2,5cm - trái 3,5cm - phải 2,5cm.
% Không dùng includehead nên "top" được đo tới mép trên phần thân bài; số trang
% nằm trong vùng lề trên, đúng như cách Microsoft Word bố trí header.
% headheight khai báo ngay tại đây để geometry tính đúng vị trí thân bài; nếu đặt
% \setlength{\headheight} sau khi nạp geometry thì thân bài bị đẩy xuống 3pt.
\usepackage[top=2.5cm, bottom=2.5cm, left=3.5cm, right=2.5cm, headheight=15pt]{geometry}

% 2c. Đáy trang KHÔNG căn đều — nguyên nhân gốc của lỗi "khoảng cách lúc lớn lúc bé".
% article.cls chỉ gọi \raggedbottom khi in một mặt; với tuỳ chọn "twoside" nó để nguyên
% \flushbottom mặc định của LaTeX. \flushbottom buộc MỌI trang phải kết thúc đúng ở lề
% dưới, và để làm được vậy TeX kéo giãn tất cả khoảng trắng dọc co giãn được có trên
% trang: khoảng giữa các đoạn (\parskip), khoảng trên/dưới công thức, khoảng quanh môi
% trường định lý, khoảng quanh hình vẽ và cả khoảng trước tiêu đề. Mỗi trang cần kéo
% giãn một lượng khác nhau nên cùng một loại khoảng cách lại to nhỏ khác nhau tuỳ trang.
% Dấu vết trong main.log: 56 dòng "Underfull \vbox ... while \output is active" — đó là
% những trang TeX không kéo giãn đủ. \raggedbottom cho phép trang kết thúc sớm hơn lề
% dưới (đúng như cách Microsoft Word xuống trang), nhờ đó mọi khoảng cách giữ đúng giá
% trị danh nghĩa và giống hệt nhau ở mọi trang.
\raggedbottom
% Khoảng giữa hai đoạn văn: cứng bằng 0, không co giãn. Mặc định của lớp tài liệu là
% "0pt plus 1pt" — phần "plus 1pt" chính là chỗ \flushbottom bấu vào để nới từng chỗ
% ngắt đoạn một ít, nên phải khử luôn để hai đoạn liền nhau bao giờ cũng cách nhau
% đúng một dòng. Các đoạn vẫn phân biệt được với nhau nhờ thụt đầu dòng (\parindent).
\setlength{\parskip}{0pt}
\usepackage{amsmath,latexsym,amscd,amsfonts,amssymb,mathrsfs,enumerate}
\usepackage{upgreek}
% Không dùng tuỳ chọn "thref" vì cả đồ án không có lệnh \thref nào; tuỳ chọn này
% định nghĩa lại \label nên nameref (do hyperref nạp sau) phải khôi phục lại định
% nghĩa gốc và in cảnh báo "The definition of \label has changed!".
\usepackage[thmmarks,standard,amsmath,hyperref]{ntheorem}
\usepackage[mathscr]{eucal}
\usepackage{makecell} % Đặt ở phần preamble
\usepackage{graphicx}
\usepackage{fancybox,cite}
\usepackage{xcolor} % Để dùng màu
\usepackage{listings} % Gói để hiển thị code
\usepackage{titlesec}
\usepackage{fancyhdr}

% 3. Cấu hình Hyperref
\usepackage[unicode,colorlinks=true, bookmarks=true, pdfstartview=FitH,]{hyperref} 
\hypersetup{
    pdftitle={Học tăng cường để tối ưu hóa khả năng tương tác của AI trong game},
    pdfauthor={Văn Thị Minh Huyền},
    colorlinks = true,
    linkcolor = blue,
    citecolor = blue,
}

\numberwithin{figure}{section}
\numberwithin{table}{section}
\numberwithin{equation}{section}

% 4. Định nghĩa font caption 13pt và nghiêng cho toàn bộ hình vẽ, bảng biểu
\DeclareCaptionFont{myfontsize}{\fontsize{13pt}{22.5pt}\selectfont}
\captionsetup{
    font={myfontsize,it}, 
    labelfont={myfontsize,it},
    justification=centering
}

% 4b. Cỡ chữ thống nhất cho phần nội dung của mọi bảng biểu.
% Lớp tài liệu khai báo 12pt nên \small cho ra 11pt, trong khi bảng không khai báo cỡ
% chữ lại thừa hưởng 13pt của thân bài; hệ quả là các bảng to nhỏ không đều nhau. Mọi
% bảng đều dùng chung lệnh dưới đây, muốn đổi cỡ chữ toàn bộ bảng biểu chỉ cần sửa
% một dòng này (ví dụ đổi sang 13pt để bằng đúng cỡ chữ thân bài).
\newcommand{\tablefont}{\fontsize{12pt}{15pt}\selectfont}

% Đổi nhãn Figure thành Hình, Table thành Bảng (cho tiếng Việt)
\renewcommand{\figurename}{Hình}
\renewcommand{\tablename}{Bảng}

% 5. Cấu hình tiêu đề (Titlesec)
% Các đề mục cấp một (Mở đầu, Cơ sở lý thuyết...) đánh số liên tục từ 1, căn trái.
\titleformat{\section}
  {\normalfont\fontsize{13pt}{22.5pt}\selectfont\bfseries}{\thesection.}{0.6em}{}
\titlespacing*{\section}{0pt}{14pt}{10pt}
\titleformat{\subsection}
  {\normalfont\fontsize{13pt}{21pt}\selectfont\bfseries}{\thesubsection}{1em}{}
% Thiếu đúng dòng \titlespacing này là lý do các tiêu đề cấp hai bị hở một khoảng lớn
% hơn cả tiêu đề cấp một: không khai báo thì titlesec giữ nguyên giá trị mặc định của
% lớp article, tức "3.25ex plus 1ex" ≈ 17pt phía trên (so với 14pt của \section) và
% phần "plus 1ex" còn co giãn được nên mỗi trang lại hở một kiểu. Đặt cứng 13pt/8pt để
% ba cấp tiêu đề giảm dần đều: 14/10 – 13/8 – 12/6.
\titlespacing*{\subsection}{0pt}{13pt}{8pt}
\titleformat{\subsubsection}
  {\normalfont\fontsize{13pt}{21pt}\selectfont\bfseries\itshape}{\thesubsubsection}{1em}{}
\titlespacing*{\subsubsection}{0pt}{12pt}{6pt}

% 5b. Tiêu đề Mục lục / Danh sách hình vẽ / Danh sách bảng: IN HOA, in đậm, căn giữa
% cho đồng bộ với DANH MỤC TỪ VIẾT TẮT và PHỤ LỤC.
% Tên tiêu đề (\contentsname, \listfigurename, \listtablename) được đặt lại ngay trước
% mỗi lệnh trong thân bài, vì polyglossia gán lại các tên tiếng Việt mỗi lần kích hoạt
% ngôn ngữ (kể cả khi đọc tệp .toc/.lof có \xpg@aux) nên khai báo ở preamble bị ghi đè.
% Cặp \hfill trước/sau tiêu đề là cách tocloft căn giữa (tiêu đề nằm trong một hộp
% rộng bằng \textwidth chứ không phải môi trường center).
\renewcommand{\cfttoctitlefont}{\hfill\large\bfseries}
\renewcommand{\cftaftertoctitle}{\hfill}
\renewcommand{\cftloftitlefont}{\hfill\large\bfseries}
\renewcommand{\cftafterloftitle}{\hfill}
\renewcommand{\cftlottitlefont}{\hfill\large\bfseries}
\renewcommand{\cftafterlottitle}{\hfill}

% 6. Cấu hình Header / Footer (Fancyhdr)
% \headheight đã được đặt 15pt qua tùy chọn của gói geometry ở mục 2.
\pagestyle{fancy}
\fancyhf{}
\fancyhead[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}

\fancypagestyle{plain}{%
  \fancyhf{}
  \fancyhead[C]{\thepage}%
  \renewcommand{\headrulewidth}{0pt}%
}

% 7. Cấu hình hiển thị code (Listings)
\definecolor{codegreen}{rgb}{0,0.6,0}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}
\definecolor{codepurple}{rgb}{0.58,0,0.82}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}

% Màu cho năm thuật toán (dùng trong biểu đồ pgfplots)
\definecolor{ppocolor}{HTML}{2196F3}
\definecolor{saccolor}{HTML}{4CAF50}
\definecolor{pocacolor}{HTML}{FF9800}
\definecolor{mappocolor}{HTML}{9C27B0}
\definecolor{dqncolor}{HTML}{E53935}

\lstdefinestyle{mystyle}{
    backgroundcolor=\color{backcolour},
    commentstyle=\color{codegreen},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    stringstyle=\color{codepurple},
    basicstyle=\ttfamily\footnotesize, % Kích thước chữ
    breakatwhitespace=false,
    breaklines=true,
    captionpos=b,
    keepspaces=true,
    numbers=left,
    numbersep=5pt,
    showspaces=false,
    showstringspaces=false,
    showtabs=false,
    tabsize=2
}
\lstset{style=mystyle} % Áp dụng style đã định nghĩa

% 8. Thiết lập môi trường Định lý, Mệnh đề, v.v. (ntheorem)
% Khoảng trắng trên/dưới mọi khối định lý – định nghĩa – nhận xét. Không khai báo thì
% ntheorem lấy \topsep, mà \topsep ở đây là 10pt PLUS 4pt MINUS 6pt thừa hưởng từ bộ
% tham số danh sách cỡ 12pt: vừa quá rộng so với thân bài, vừa co giãn được nên khối
% Định nghĩa 3.1 và Định nghĩa 3.2 hở ra hai mức khác nhau. 6pt cứng cho cả trên lẫn
% dưới là đủ tách khối định nghĩa khỏi văn xuôi mà không tạo lỗ hổng giữa trang.
% HAI DÒNG NÀY PHẢI ĐỨNG TRƯỚC MỌI LỆNH \newtheorem: ntheorem nướng giá trị skip vào
% định nghĩa của từng môi trường ngay tại thời điểm \newtheorem, đặt sau sẽ không có
% tác dụng với các môi trường đã khai báo.
\theorempreskip{6pt}
\theorempostskip{6pt}
\newtheorem{tc}{Tính chất}[section]
\newtheorem{md}{Mệnh đề}[section]
\newtheorem{bd}{Bổ đề}[section]
\newtheorem{hq}{Hệ quả}[section]
\newtheorem{dl}{Định lý}[section]
 
\allowdisplaybreaks

% Môi trường định nghĩa
\theoremheaderfont{\bfseries}
\theorembodyfont{}
\theoremseparator {.}         
\newtheorem{dn}{Định nghĩa}[section]

% Môi trường Ví dụ
\theoremheaderfont{\bfseries}
\theorembodyfont{\normalfont}
\theoremseparator {.}         
\newtheorem{vd}{Ví dụ}[section]

% Môi trường Chú ý
\theoremstyle{plain} 
\theoremheaderfont{\bfseries}
\theorembodyfont{\normalfont}
\theoremseparator {.}         
\newtheorem{cy}{Chú ý} 

% Môi trường Bài tập
\theoremheaderfont{\bfseries}
\theorembodyfont{\normalfont}
\theoremseparator {.}         
\newtheorem{bt}{Bài tập} 

% Môi trường nhận xét
\theoremheaderfont{\bfseries}
\theorembodyfont{\normalfont}
\theoremseparator {.}         
\newtheorem{nx}{Nhận xét}[section] 

% Môi trường định lý
\theoremstyle{plain} 
\theoremheaderfont{\normalfont\bfseries}
\theorembodyfont{\normalfont}
\theoremseparator {.}         
\renewtheorem{theorem}{Định lý} 

% Môi trường Mệnh đề
\theoremstyle{plain}
\theoremheaderfont{\normalfont\bfseries}
\theorembodyfont{\normalfont}
\theoremseparator {.}         
\renewtheorem{proposition}{Mệnh đề} 

% Môi trường Chứng minh
\theoremstyle{nonumberplain}  
\theoremheaderfont{\normalfont\bfseries}
\theorembodyfont{\normalfont}
\theoremseparator{.} 
\theoremsymbol{\ensuremath{\square}}
\newtheorem{cm}{Chứng minh}[section]

% 9. Cỡ chữ chuẩn: Times New Roman 13pt, dãn dòng 1,5 lines (22,5pt)
% Lớp article chỉ hỗ trợ tối đa 12pt nên phải định nghĩa lại \normalsize. Nếu chỉ gọi
% \fontsize ở đầu mỗi tệp thì nội dung trong float (bảng, hình) vẫn quay về 12pt do
% \@floatboxreset gọi \normalsize.
\makeatletter
\renewcommand\normalsize{%
  \@setfontsize\normalsize{13pt}{22.5pt}%
  \abovedisplayskip 12\p@ \@plus3\p@ \@minus7\p@
  \abovedisplayshortskip \z@ \@plus3\p@
  \belowdisplayshortskip 6.5\p@ \@plus3.5\p@ \@minus3\p@
  \belowdisplayskip \abovedisplayskip
  \let\@listi\@listI}
\makeatother
\normalsize

% Cho phép TeX nới nhẹ khoảng trắng ở những đoạn khó ngắt dòng (tên hàm, định danh
% mã nguồn dài) thay vì để chữ tràn ra ngoài lề. Không ảnh hưởng mật độ chữ thông thường.
\setlength{\emergencystretch}{3em}

% ================= BẮT ĐẦU TÀI LIỆU ================= %
\begin{document}

\begin{titlepage}
\thispagestyle{empty}
\null

% ==================== BÌA (dựng lại đúng theo mẫu BM.ĐT.19.06 – Bìa.docx) ====================
% Mẫu Word dựng bìa bằng một bảng hai cột (cột trái hẹp đựng chữ dọc) nên khung, vạch
% dọc và mọi dòng chữ đều nằm ở toạ độ cố định so với mép giấy A4, không phụ thuộc lề
% thân bài. Vì vậy toàn bộ bìa được vẽ bằng một tikzpicture đặt theo toạ độ tuyệt đối
% tính từ góc trên–trái của trang giấy: trục x hướng sang phải, trục y hướng xuống.
% Mọi con số dưới đây đo trực tiếp trên bản PDF do Word xuất ra từ Bìa.docx.
%
%   Khung (nét 0,5pt): mép trái 1,952cm – mép phải 20,337cm
%   Vạch dọc chia cột: x = 3,891cm (cột trái rộng 1,94cm)
%   Trục căn giữa của cột nội dung: x = 12,114cm
%
% Riêng chiều cao khung KHÔNG lấy theo Bìa.docx: bảng trong mẫu Word chỉ cao tới
% 25,408cm nên chừa một khoảng trắng lớn ở đáy trang. Khung dưới đây được kéo dài
% ra sát mép giấy (cách mép trên và mép dưới 1,5cm) cho giống bìa mẫu in thực tế;
% muốn đổi độ dài khung chỉ cần sửa \biaTop và \biaBottom, nội dung bên trong
% không bị ảnh hưởng vì mỗi dòng đều có toạ độ riêng.
%   Chữ dọc cột trái: chân chữ tại x = 2,554cm, xoay 90 độ theo chiều kim đồng hồ
%                     (đọc từ trên xuống), cỡ 13pt và KHÔNG in đậm
%   Cỡ chữ: 13pt cho tên bộ/tên trường, khối thông tin sinh viên và dòng địa danh –
%           20pt cho "ĐỒ ÁN TỐT NGHIỆP" – 16pt cho tên đề tài
%   Logo: ảnh vuông 6,251 x 6,251cm, mép trên cách đầu trang 5,309cm
%
% Các số -y bên dưới là khoảng cách từ mép trên trang giấy tới ĐƯỜNG CHÂN CHỮ của dòng.
\def\biaLeft{1.952}      % mép trái khung (cm, tính từ mép trái trang giấy)
\def\biaRight{20.337}    % mép phải khung
\def\biaDivider{3.891}   % vạch dọc chia cột trái
\def\biaTop{1.5}         % mép trên khung (cm, tính từ mép trên trang giấy)
\def\biaBottom{28.2}     % mép dưới khung
\begin{tikzpicture}[remember picture, overlay,
      every node/.style={inner sep=0pt, outer sep=0pt}]

  \coordinate (bia) at (current page.north west);

  % --- Khung bao và vạch dọc chia cột, nét 0,5pt ---
  \draw[line width=0.5pt]
      ($(bia)+(\biaLeft cm,-\biaTop cm)$) rectangle ($(bia)+(\biaRight cm,-\biaBottom cm)$);
  \draw[line width=0.5pt]
      ($(bia)+(\biaDivider cm,-\biaTop cm)$) -- ($(bia)+(\biaDivider cm,-\biaBottom cm)$);

  % --- Cột trái: họ tên ở phần trên, ngành ở phần dưới (13pt, không in đậm) ---
  \node[rotate=-90, anchor=base west] at ($(bia)+(2.554cm,-3.552cm)$)
      {\fontsize{13pt}{15pt}\selectfont VĂN THỊ MINH HUYỀN};
  \node[rotate=-90, anchor=base west] at ($(bia)+(2.554cm,-16.644cm)$)
      {\fontsize{13pt}{15pt}\selectfont KHOA HỌC MÁY TÍNH};

  % --- Tên bộ và tên trường (13pt in đậm), tên trường có gạch chân 0,5pt ---
  \node[anchor=base] at ($(bia)+(12.114cm,-3.870cm)$)
      {\fontsize{13pt}{15pt}\selectfont\bfseries BỘ GIÁO DỤC VÀ ĐÀO TẠO};
  \node[anchor=base] (truong) at ($(bia)+(12.114cm,-4.485cm)$)
      {\fontsize{13pt}{15pt}\selectfont\bfseries ĐẠI HỌC PHENIKAA};
  \draw[line width=0.5pt]
      ([yshift=-0.125cm]truong.base west) -- ([yshift=-0.125cm]truong.base east);

  % --- Logo trường ---
  \node[anchor=north] at ($(bia)+(12.114cm,-5.309cm)$)
      {\includegraphics[width=6.251cm]{image/Logo_1.jpg}};

  % --- Tên loại đồ án (20pt in đậm) ---
  \node[anchor=base] at ($(bia)+(12.114cm,-12.308cm)$)
      {\fontsize{20pt}{23pt}\selectfont\bfseries ĐỒ ÁN TỐT NGHIỆP};

  % --- Tên đề tài (16pt in đậm, hai dòng) ---
  \node[anchor=base] at ($(bia)+(12.114cm,-13.718cm)$)
      {\fontsize{16pt}{18pt}\selectfont\bfseries HỌC TĂNG CƯỜNG ĐỂ TỐI ƯU HÓA};
  \node[anchor=base] at ($(bia)+(12.114cm,-14.450cm)$)
      {\fontsize{16pt}{18pt}\selectfont\bfseries KHẢ NĂNG TƯƠNG TÁC CỦA AI TRONG GAME};

  % --- Khối thông tin sinh viên: nhãn in đậm, giá trị in đậm nghiêng, cỡ 13pt.
  % Nhãn cột trái bắt đầu ở x = 6,029cm. Cột phải trong mẫu Word được đẩy bằng dấu
  % cách nên "Khóa" (13,926cm) và "Hệ" (13,837cm) lệch nhau 0,9mm; muốn cho thẳng
  % hàng chỉ cần sửa hai con số này về cùng một giá trị.
  \node[anchor=base west] at ($(bia)+(6.029cm,-16.963cm)$)
      {\fontsize{13pt}{15pt}\selectfont\bfseries Sinh viên:\hspace{6.5pt}\itshape Văn Thị Minh Huyền};
  \node[anchor=base west] at ($(bia)+(6.029cm,-17.856cm)$)
      {\fontsize{13pt}{15pt}\selectfont\bfseries Mã số sinh viên: \itshape 22010329};
  \node[anchor=base west] at ($(bia)+(13.926cm,-17.856cm)$)
      {\fontsize{13pt}{15pt}\selectfont\bfseries Khóa: \itshape 16};
  \node[anchor=base west] at ($(bia)+(6.029cm,-18.758cm)$)
      {\fontsize{13pt}{15pt}\selectfont\bfseries Ngành: \itshape Khoa học máy tính};
  \node[anchor=base west] at ($(bia)+(13.837cm,-18.758cm)$)
      {\fontsize{13pt}{15pt}\selectfont\bfseries Hệ: \itshape Chính quy};
  \node[anchor=base west] at ($(bia)+(6.029cm,-19.660cm)$)
      {\fontsize{13pt}{15pt}\selectfont\bfseries Giảng viên hướng dẫn: \itshape ThS. Nguyễn Văn Sơn};

% --- Địa danh và năm bảo vệ (13pt in đậm) ---
  \node[anchor=base] at ($(bia)+(12.114cm,-26.800cm)$)
      {\fontsize{13pt}{15pt}\selectfont\bfseries Hà Nội – 2026};

\end{tikzpicture}
\end{titlepage}
 


 %%%%%%%%%%%%%%%%%%%%
% Cỡ chữ phần nội dung: Times New Roman 13pt, giãn dòng 1,5 (22,5pt)
\fontsize{13pt}{22.5pt}\selectfont
\pagestyle{fancy}
\fancyhf{} 
\hypersetup{linkcolor=black}

% Chèn mục lục
\pagenumbering{roman}
\fancyhead[C]{\thepage} 
\renewcommand{\headrulewidth}{0pt}  

\input{sec1_intro}
\newpage
\input{sec_1_5}
\newpage
\input{sec_abstract}
\newpage

\fancyhead[C]{\thepage}
\renewcommand{\contentsname}{MỤC LỤC}
\tableofcontents

\newpage
\pagestyle{fancy}
\fancyhead[C]{\thepage}
\renewcommand{\listfigurename}{DANH SÁCH HÌNH VẼ}
\listoffigures

\newpage
\renewcommand{\listtablename}{DANH SÁCH BẢNG}
\listoftables

\newpage
\input{sec_abbrev}

\newpage
% Ghi lại số trang La Mã cuối cùng của phần đầu để phần sau Kết luận đánh số tiếp.
\newcounter{romanpage}
\setcounter{romanpage}{\value{page}}
\pagenumbering{arabic}
\setcounter{page}{1}

% Cài lại fancy cho phần nội dung
\pagestyle{fancy}
\lhead{}
\chead{\thepage}
\rhead{}
\lfoot{}
\cfoot{}
\rfoot{}
\renewcommand{\headrulewidth}{0pt}  

\input{sec2_new_risk}
\input{sec3_opt_strategy}
\input{sec5}
\input{sec6}
\input{sec6_5_app}
\input{sec7}

\newpage
% Hết phần Kết luận: các phần còn lại đánh số bằng chữ số La Mã viết thường,
% nối tiếp dãy số La Mã của phần đầu đồ án.
% romanpage đã giữ sẵn số La Mã kế tiếp chưa dùng (sau \newpage bộ đếm page đã tăng),
% nên gán thẳng, không cộng thêm 1.
\pagenumbering{roman}
\setcounter{page}{\value{romanpage}}

\renewcommand{\refname}{Tài liệu tham khảo}
\bibliographystyle{plain}
\bibliography{ref}

\newpage
\input{appendix}

\end{document}